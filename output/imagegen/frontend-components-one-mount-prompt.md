# One mount, multiple components

Tool: built-in image_gen. Edited technical component illustration.
Asset: `starter/public/memes/frontend-components-one-mount.png`.

Revise the reference conference slide. Keep its 16:9 landscape canvas, navy #212737, pink #ff6bed, white typography, clean technical diagram aesthetic, NO mascots, NO animals. The main message must become: an isolated test can mount only a single Button, while an integration test mounts ONE parent Shop and thereby includes its REAL nested child components and tests their collaboration.
Keep heading exactly "Im Frontend testen wir Komponenten". Subtitle "Eine Komponente mounten – unterschiedlich viel testen".
Use left column about 35% width, right column 60% width, generous spacing. Left heading "Isoliert", right heading "Integriert".
LEFT: short monospace text "mount(Button)" above a downward mounting arrow into ONE component boundary labeled "Button". Inside just one rendered pink button "Kaufen", with cursor clicking it, and output arrow pointing to "click-Event". No ProductCard, no Shop, no other component in left. Caption "Reagiert der einzelne Button?"
RIGHT: monospace "mount(Shop)" above ONE mounting arrow to a large parent boundary labeled "Shop". Inside show an unmistakable nested component tree drawn as CONTAINMENT: left child box labeled "ProductCard", and INSIDE that box a smaller nested component box labeled "Button" with a pink "Kaufen" button and click cursor. Beside ProductCard, inside Shop, a separate sibling component box labeled "Cart" containing a nested child box labeled "CartItem" with a simple text row "1 Artikel". Another small sibling component box labeled "CartBadge" shows "1". Draw pink interaction arrows from Button across to Cart and CartBadge, labeled together with a single shared legible caption "Klick → gemeinsamer Zustand → UI-Update". These are conceptual behavior arrows, not direct imports or direct sibling component method calls. No separate state cylinder, no backend, no database. Show the nesting clearly: Shop contains ProductCard (contains Button), Cart (contains CartItem), and CartBadge. Each is an actual component. Only Shop is directly mounted by test, NEVER put separate mount calls above child components. Use a small label inside Shop "Echte Kindkomponenten". Caption below right "Ein Klick aktualisiert mehrere Komponenten."
Footer exact "Ein Mount. Mehrere echte Komponenten. Ihr Zusammenspiel im Test."
Prioritize technical clarity and large readable labels. No extra text, no source code beyond the two mount labels, no animals, no decorative scene, no watermark. Make it clear the isolated Button is the SAME Button nested under ProductCard in integrated example. Retain enough breathing room that all component names, click path and containment are immediately understandable on stage.
